#include <stdio.h>
using namespace std;
class node{
	int data;
	node* left;
	node* right;
}
void insert(node* root,int data)
int main(){
 	node* root;  //pointer to root node
	root= NULL;  //setting tree as empty i.e. no children
}
